# Experiment config (per-user, per-experiment)
cluster.toml

# Build artifacts (remote only, but just in case)
build/
/rdma
*.o
.setup-done
compile_commands.json

# IDE
.idea/
cmake-build-*/

# System headers copied for IDE support
include/infiniband/

# Results
results/

# OS
.DS_Store
